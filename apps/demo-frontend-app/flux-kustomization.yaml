apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: demo-frontend-app
spec:
  interval: 10m0s
  path: ./
  postBuild:
    substitute:
      ENVIRONMENT_NAME: feat-5
  prune: true
  sourceRef:
    kind: OCIRepository
    name: demo-frontend-app
  targetNamespace: demo-app
